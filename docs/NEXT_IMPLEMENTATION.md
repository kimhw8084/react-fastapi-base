# Remaining implementation — continue from 0.2.0-lab

Do not restart, rename, discard the working widgets, or claim the full scope is finished. The product is `react-fastapi-base`; SysGrid is reference-only. Preserve source/checkpoints from the beginning of every run.

## 1. Close the execution gap

Resolve and review candidate frontend dependencies/lock in a network-enabled environment. Execute React strict typing, component tests, `/lab` iframe loading under production CSP, controlled widget model/prop updates, Storybook build, Vite production build, real HTTP browser workflows and dependency advisories. The current native-widget code has its own tested path; this is not permission to replace React qualification with screenshots.

## 2. Close data integration and customization

Bind selected workspaces to the existing FastAPI services through generated API contracts. Prove errors, stale revisions, tenant switch, navigation dirty-state guards, audit and permissions. Add the pending model-specific presentation controls and scoped theme export/import. Prevent industry data/business labels from entering generic primitives.

## 3. Certify families, not file counts

The catalog lists 605 required items with explicit maturity. Extend the current 23 families incrementally: component implementation, runtime contract, Storybook story, Lab scenario, non-drag interaction, light/dark visuals, accessibility, persistence where meaningful, stress benchmarks, documentation. Mark stable only after actual relevant evidence; do not remove required entries to improve the score.

## 4. Finish advanced engineering capabilities

Evaluate/license real advanced grid, chart, Gantt/calendar, diagram and editor engines as originally scoped. Current bounded native examples are reusable but not substitutes for server-side infinite grids, pivot/range editing, critical path/resource leveling, validated SPC rules, industrial wafer/reticle formats, EDA/device protocol integrations, authorable graphs or distributed schedulers. Keep an available baseline and paid integrations optional.

## 5. Finish platform and release operations

Add safe upgrade apply/rollback, domain-specific generators, jobs/realtime/integrations, richer admin/team scopes, strong accessibility/security/performance gates and exact-version reproducible builds. Complete the pinned SysGrid audit, reusable-pattern matrix and an independently run behavioral comparison. Run a fresh macOS archive setup and production-like React/FastAPI execution.

## 6. Qualify company infrastructure

Verify AccessKey execution identity with simultaneous real users and the actual PaaS routing contract. Obtain the storage provider's SQLite/locking/fsync/rename/durability support and topology limits; then run non-destructive qualification on disposable data. A probe alone does not prove distributed filesystem correctness. Test two native publications, restart/redeploy persistence, controlled migrations and isolated restoration. Never work against company data merely because the template booted locally.
